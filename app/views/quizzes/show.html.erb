<div class="container">
  <h2><%= @quiz.title %></h2>

  <%= render_quiz_component(@quiz) %>

  <p>
    <small>
      Created on <%= @quiz.created_at.to_formatted_s(:db) %>
      by <%= @quiz.user.name %>
    </small>
  <p>

</div>
